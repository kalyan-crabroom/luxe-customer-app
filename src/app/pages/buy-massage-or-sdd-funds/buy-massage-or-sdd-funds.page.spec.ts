import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BuyMassageOrSddFundsPage } from './buy-massage-or-sdd-funds.page';

describe('BuyMassageOrSddFundsPage', () => {
  let component: BuyMassageOrSddFundsPage;
  let fixture: ComponentFixture<BuyMassageOrSddFundsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BuyMassageOrSddFundsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
